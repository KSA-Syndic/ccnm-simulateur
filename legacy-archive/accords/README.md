# Accords (archive JS)

Copie historique des définitions d’accords au format **JavaScript** (`KuhnAgreement.js`).

**Référence actuelle du projet :** `src/accords/` (TypeScript) — voir `src/accords/README.md` et `docs/AJOUTER_ACCORD.md`.

Ce dossier n’est utilisé que si vous conservez `legacy-archive/` pour l’oracle ou le dual-run.
